version: '3'
services:
  exefree:
    build: .
    container_name: exefree
    volumes:
      - ./workspace:/workspace
    environment:
      - DISPLAY=host.docker.internal:0.0
    ports:
      - 1337:1337
    stdin_open: true
    shm_size: 2g
    tty: true
